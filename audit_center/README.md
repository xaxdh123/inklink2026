# 审核中心

## 模块简介
审核中心子程序（功能待补齐）。

## 入口与目录
- 子程序键名：`AuditCenter`
- 子目录：`audit_center`
- 可执行文件：`audit_center.exe`

## 打包/运行路径
- 运行：`./bin/audit_center.exe`

## 补丁包路径
- `./new_version/audit_center/`（由 COS 对象存储同步）

## 地址列表
- 统一维护于项目根目录 `README.md` 的地址列表章节。