# 三方工具

## 模块简介
三方工具子程序（功能待补齐）。

## 入口与目录
- 子程序键名：`ThirdParty`
- 子目录：`third_party`
- 可执行文件：`third_party.exe`

## 打包/运行路径
- 运行：`./bin/third_party.exe`

## 补丁包路径
- `./new_version/third_party/`（由 COS 对象存储同步）

## 地址列表
- 统一维护于项目根目录 `README.md` 的地址列表章节。